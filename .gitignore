# macOS
.DS_Store

# Claude Code local settings (contains API tokens — never push these)
.claude/

# Local dev server
server.py

# Analytics — keep API key off GitHub, add file manually on Netlify
posthog.js
